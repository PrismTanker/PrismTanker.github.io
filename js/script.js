$(document).ready(function () {
    // Make user events availible
    $("#initial_prompt>button").on("click", clear_prompt)
    console.log("If you are reading this then I somehow got the page to load without another catastrpohic failure")
})